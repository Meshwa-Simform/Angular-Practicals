import { MaskPhonenuPipe } from './mask-phonenu.pipe';

describe('MaskPhonenuPipe', () => {
  it('create an instance', () => {
    const pipe = new MaskPhonenuPipe();
    expect(pipe).toBeTruthy();
  });
});
